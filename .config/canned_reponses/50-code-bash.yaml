responses: 
- name: code_bash_read_stdin
  text: | 
    while IFS= read -r line 
    do 
        echo -e "${line}"
    done

- name: code_bash_remove_path 
  text: |
    IFS=':' p=($PATH)
    p=(${p[@]%%*linuxbrew*})
    IFS=':' echo "${p[*]}"

