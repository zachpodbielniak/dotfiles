#!/bin/bash 
SLEEP_INTERVAL=${SLEEP_INTERVAL:-30}
SLEEP_REQUESTS=${SLEEP_REQUESTS:-5}
yt-dlp \
    -ci \
    --merge-output-format mkv \
    --audio-format vorbis \
    -f bestaudio+bestvideo \
    --cookies ~/.cookies/cookies.Media.txt \
    --sleep-interval ${SLEEP_INTERVAL} \
    --sleep-requests ${SLEEP_REQUESTS} \
    "${@}"

