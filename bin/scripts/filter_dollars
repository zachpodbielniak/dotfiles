#!/bin/bash 
total=0

while IFS= read -r line 
do 
    echo "$line"
    if [[ "$line" =~ \$([0-9]+\.[0-9]+) ]]; then 
        total=$(bc <<< "$total+${BASH_REMATCH[1]}")
    fi
done
echo ""
echo "TOTAL: \$$total"
