#!/bin/bash
set -euo pipefail

if [[ ${#} -lt 1 ]]
then 
    echo -e "Use this tool correctly:"
    echo -e "\t$0 <device>"
fi

DEVICE="${1}" 
UUID=$(sudo cryptsetup luksUUID "${DEVICE}")

# Do the open 
sudo cryptsetup open "${DEVICE}" "luks-${UUID}"

