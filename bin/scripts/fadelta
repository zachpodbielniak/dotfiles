#!/bin/bash 
# filter by adding days delta
set -euo pipefail

if [[ $# -lt 1 ]]
then 
    echo "usage:"
    echo "$(basename ${0}) <days_delta>"
fi

while IFS= read -r line 
do 
    echo "${line} ($(days_delta ${1}))"
done


