#!/usr/bin/env perl
use strict;
use warnings;
use Time::Piece;

my $cur_date = Time::Piece->strptime(localtime->strftime("%Y-%m-%d"), "%Y-%m-%d");
my $date_past = Time::Piece->strptime($ARGV[0], "%Y-%m-%d");

my $secs_diff = $cur_date->epoch - $date_past->epoch;
my $days_diff = $secs_diff / (24*60*60);

printf("%d\n", $days_diff);

