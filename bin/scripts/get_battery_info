#!/bin/bash 

if [[ "${HOSTNAME}" == "lt-zach" ]]
then
    upower -i /org/freedesktop/UPower/devices/battery_BAT1
elif [[ "${HOSTNAME}" == "hacbook" ]]
then
    upower -i /org/freedesktop/UPower/devices/battery_macsmc_battery
fi

