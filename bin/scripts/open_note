#!/bin/bash 
set -euo pipefail

notes_dir="${HOME}/Documents/notes"
note_to_open=""
note_to_open=$(bash -c "cd ${notes_dir} && fzf")

if [[ "${note_to_open}" != "" ]]
then 
    cd "${notes_dir}"
    exec nvim "${note_to_open}"
fi

