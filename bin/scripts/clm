#!/bin/bash

if [ $# -lt 1 ]; then 
    echo "Use this correctly!"
    echo "\t$0 <column>"
    exit 1
fi

COL="$1"

while IFS= read -r line 
do 
    echo "$line" | awk "{ printf \"%s\n\", \$${COL} }"
done

