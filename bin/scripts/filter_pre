#!/bin/bash 

if [[ $# -lt 1 ]]; then 
    echo -e "Use this tool correctly"
    echo -e "\t$0 <prepend> [append]"
fi

prepend="$1"
append=""

if [[ $# -ge 2 ]]; then 
    append="$2"
fi

while IFS= read -r line 
do 
    echo -e "${prepend}${line}${append}"
done

